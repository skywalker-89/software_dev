google auth
once registed with google the firstname and lastname name will be stored in the data base 
 fetch first name and last name from google
 email and phone from google too 
and users can login with google again and again 

$$$ gmail and password for our gmail acc 
 GMAIL : returneedeveloper@gmail.com 
 PASS : Returnee2025


before letting the user post lost or found item users must login into the app first 

30 jan 2025
connect the front end part of the code with the backend

while the post is uploading make screen white blur abit and show the loading animation

once the uploading is done dont just alert the user by normal web
notify users by green bubble that will appear once the uploading is done.


set up post found item route and connect it to the front end 

fetch all the posts on to the home page and mark all the lost and found items on to the homepage map 
and showing only the items that shows up on the map there. (near by location)

move the default map to the current location

list all the posted uncliaim items onto the search result 
and configure the mapping part to to list only the item that are unclaim 

make the item cards clickable and navigate to the item page 
    -design the item page 
        - adjusting the layouts and having zoom in zoom out in the pictures too

    - mark map and zoom in picture


claim the item is done (Lost)



make another dedicated page for Return The item (found)

if the users want to return the item they have to send the 


gmail or sms sent to the user and they can set pick up time for that.

once both users agree to return and receive things the receiver set the time and place to pick up the thing.


in order to claim the item users need to id verification first 
    - add another column inside of the users verified and unverify and showing the satus at the account page 
    - add another column in the "user" table called verification (verified/not verified)
    - request claim if verified 
        can get in easily
    - if not  verifired 
        navigate to the verification page first and notify user that please verify before claiming item.

leave at the infomation center of the places. (function)
- design the infomation center reciever return website too.

once i clicked on the button should i navigate to another page 

lost item : 
click Request Claim -> Select Time and Place of to return things -> Chat Created -> Email sent to the returner 
-> in that email if that user agree to use to the time and place they can click in that email if 
they dont they can talk to the receive person there by via click link in the gmail sent to chat 
with another user for another time and place. -> 
once both users agreed on the time -> the system generate a qr code for the request claim user. 
-> users met scan qr and got the thing. 

likely match algorithm (basic first it can be advance later on)


pick up location field can be up to the conversation ot any infomation centers near the area


*** for chatting use MessageQeuue and web socket *** 
*** for emailing i am going to use nodemailer ***
    -adjusting the email notification of the website using Toaster instead of alert 

For Returning stuffs make and Request form that ask the user to put the picture into it too. (Return Request)


*** chat system ***
use Mongo DB to store the chat logs 

Final Answer?
✨ Your system is a mix of both:
✅ Pub/Sub for real-time messaging (when both users are online).
✅ Queue Messaging for offline messages (when the recipient is unavailable).

This setup ensures instant messaging, message reliability, and smooth UX! 🚀🔥

once a user clicked on verify button there the id card and face pics will be sent onto the cloudinary.


create another mechanism if a user is not verified they cant send the email to the founder or the looser of the items 
    > create another column in the user table "verified" the statuses will be "verified" and "not_verified" (psql)
    > the admin of the app work here. 
    stages that user will see 
        > verified 
        > pending 
        > please upload the picture again (unclear photos)
        > blank (inputs are open)
        > please upload the picture again (unclear photos)
    >* redesign the statuses designs 


> work on the chatting system *** 


> user need to ne verified first in order to chage their email and password (work on this 22/02/2025)
    ^^^ user need to be verified first inorder to have be able to send the request on the request claim or request return button
        **** if the user is not verified just pop up a modal saying please verified first in order to send the requests.

> create a link in gmail to navigate to chat. that created *****
    ***** if this item is yours click here confirm here if you are free at this ..... confirm here type of button if not 
        just click chat
    (prevent opening the new tab.)

    ^^^^ chat room with another user created when B response to A request if he is not ok with the location and the time.
> once a user clicked on chat button in the gmil navigate to the chat room of that user with the poster.


> set up socket.io(pub/sub) and for live chat in the front end save chat logs in mongoDB the messages that were sent in live will be stored
in mogoDB as read message but the non live chat will be handle as (queue message)

>>> adjust the socket.io to just send the messages via the participantsNUMIDs

>>> save the messages into the MONGODB and adjust the frontend recieving and sending layouts.;

>>>> fix the double logged in.

>> make read and unread status for the message too <<<
    >>> add the configure the chatschema 
        >>> add seen and unseen to message 
        >>> put red dot there at the right side of the chat in chatList with th number of unseen message (max 99+)
        >>> if the chat is called or the message is read in live the status of those messages will be seen 
        >>> there will be a word down there for showing people that the chat is seen 


>>> make the chat able to send the pictures
pic -> cloudinary -> url(cloudinary) -> display it in the chat <<<<

fix the image sending make sure i can send multiple images at a time in frontend
    >>> if i selected a pic i should be able to click the send button (done)
    >>> fix the image layout there in the chat (done)
    >>> fix the review pictures there make it more beautiful (done)
    >>> fix the read and unread status there in the chat and chatList
    >>> loading animation while uploading images too (done)
    >>> click into the image tooo (done)

tmr 07/03/2025 
    >>>>> enable reschudle in the chat. (done)
    >>>>> generate qr code by user info and item id.11111(done)
    todo(now) 08/03/2025
        >>>> sender_id and item_id matched == pass (done)
        >>>> make match in the backend if the item is matched send the signal to the backend to change the status (done)
            **** create backend function and route to handle this. (done)
            >>>> change the status and the time claim at there (done)

    >>>>> the item there will be saved as the item history of the poster
    >>>>> delete the item lost/found post if the final qr code is scanned (done)


    

sunday 09/03/2025 
    >>>> every url must log in first 
    >>>> **** 5 kms around the map items and filters

pass in chatId search for participantsNUMIDs and fetch their everyinfo of the user and chatTitle to make a gmail confirmation

***** generate the qr code in gmail and enable qr code scan ******

